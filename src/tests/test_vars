# defined to 1 if subunit is enabled
ENABLE_SUBUNIT=0
export ENABLE_SUBUNIT
EXEEXT=.exe
export EXEEXT
HAVE_FORK=1
export HAVE_FORK

# path of the tests directory
if [ x"../../3rd-part/check-0.15.2/src/../tests" != x"." ]; then
    if [ -z "@IS_MSVC@" -o "@IS_MSVC@" != "1" ]; then
       SRCDIR="../../3rd-part/check-0.15.2/src/../tests/"
    else
       SRCDIR="../../3rd-part/check-0.15.2/src/../tests\\"
    fi
else
    SRCDIR=""
fi

export SRCDIR
